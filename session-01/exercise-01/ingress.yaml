apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: wp-ingress
  annotations:
    nginx.ingress.kubernetes.io/affinity: cookie
spec:
  rules:
  - host: wp-ex01.bar.com
    http:
      paths:
      - path: /
        backend:
          serviceName: wp
          servicePort: http
